# @citis/sdk

The REST client generated from the API's OpenAPI document will live here. The
foundation keeps the boundary separate so portals never duplicate transport,
error-envelope, or session handling logic.