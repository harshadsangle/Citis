{
  "name": "citis-infotech",
  "version": "1.0.0",
  "private": true,
  "description": "CITIS InfoTech EdTech PWA monorepo",
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev": "concurrently --names frontend,backend --prefix-colors cyan,green \"npm run dev:frontend\" \"npm run dev:backend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend && npm run build --prefix strapi-cms",
    "lint": "npm run lint --prefix frontend && npm --prefix backend exec tsc -- --noEmit"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  },
  "engines": {
    "node": ">=20"
  }
}
