const navigation = ["Dashboard", "Institution profile", "Campuses", "Users", "Roles & permissions"];

export default function InstitutionAdminPage() {
  return <main className="portal"><aside className="sidebar"><div className="brand">CITIS <span>Education</span></div><p className="eyebrow">Institution portal</p><nav>{navigation.map((item) => <a href="#" key={item}>{item}</a>)}</nav></aside><section className="content"><p className="eyebrow">Foundation workspace</p><h1>Institution administration</h1><p className="muted">A secure workspace for institution profiles, campuses, users, and scoped permissions.</p><div className="grid">{[["Institution profile", "Connect your institution identity"], ["Campuses", "Add and manage campus locations"], ["Users", "Invite educators, staff, learners, and parents"], ["Access control", "Assign roles with database-backed permissions"]].map(([title, copy]) => <article key={title}><h2>{title}</h2><p className="muted">{copy}</p><a className="action" href="#">Open workspace →</a></article>)}</div><div className="notice"><strong>Tenant-scoped by default</strong><span>Institution data is isolated by the authenticated tenant and campus context.</span></div></section></main>;
}